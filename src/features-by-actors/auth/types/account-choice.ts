export interface IAccountChoice {
    id: number;
    illustration: string;
    activeIllustration: string;
    title: string;
    subtitle: string;
    isSelected: boolean;
}

export interface IBuyerType {
    id: number;
    label: string;
    isSelected: boolean;
}